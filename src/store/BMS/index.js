/* 后台管理数据存放地 */

// state:仓库存储数据的地方
const state={
    name:"张三"
};

// mutations:修改state的唯一手段
const mutations={

};

// actions:处理action,可以书写自己的业务逻辑,也可以处理异步
const actions={

};

// getters:理解为计算属性,方便简化数据仓库,让组件获取仓库数据更加简单
const getters={

};

export default{
    state,
    mutations,
    actions,
    getters
}